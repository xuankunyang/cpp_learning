#include <iostream>
#include <set>
#include <algorithm>

using namespace std;;
using ll = long long;


int main()
{
	ios::sync_with_stdio, cin.tie(0), cout.tie(0);
	int q;
	cin >> q;
	set<ll>s;
	for (int i = 1; i <= q; ++i)
	{
		int op;
		ll x;
		cin >> op>>x;
		
	}
	////fufufu




	return 0;
}